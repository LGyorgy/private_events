<h1>Create an invitation for <%= @event.title %> event</h1>
<%= form_tag(invitations_path, method: 'post') do %>
  <%= label_tag "User email" %>
  <%= email_field_tag :email %>
  <%= hidden_field_tag :event_id, @event.id %>
  <%= submit_tag "Send Invitation" %>
<% end %>